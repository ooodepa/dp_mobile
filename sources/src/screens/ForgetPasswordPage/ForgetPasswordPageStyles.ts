import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  wrapper__content: {
    width: '100%',
    flex: 1,
  },
  wrapper__footer: {
    width: '100%',
  },
});

export default styles;
